version: '3'
services:
  api:
    tty: true
    stdin_open: true
    build: .
    command: /bin/sh -c "rm -f /capo_srlsoralu/tmp/pids/server.pid && bundle exec rails s -p 8000 -b '0.0.0.0'"
    volumes:
      - .:/app_name
    ports:
      - "8000:8000"
